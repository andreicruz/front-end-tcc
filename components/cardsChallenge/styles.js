import { StyleSheet } from 'react-native';

const border = '#d6d6d6';

export const styles = StyleSheet.create({
    card: {
        borderColor: border,
        borderWidth: 1,
        borderRadius: 20,
        marginTop: 40,
    },
});